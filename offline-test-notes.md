# Offline smoke-test notes

The packaged entry point loads at `file:///home/ubuntu/dynamo-exporter-docs/offline-package/dist/index.html` and the browser console shows React initialization info without a reported exception, but the browser viewport is blank and no interactive elements are detected. The production HTML now references `./assets/index-C2_Y_2Sf.js` via a non-module script tag and `./assets/index-DoUh6WCl.css` is not present because the IIFE build inlined CSS. The next diagnosis should inspect the saved browser HTML and browser-side DOM after the script runs; direct local-file execution may be restricted by the browser harness even though the bundle is self-contained.

The direct-file test succeeded after converting the Vite output to an IIFE, removing the hosted preview runtime, and deferring the script until after the root element is parsed. `file:///.../dist/index.html` renders the home page, and `file:///.../dist/index.html#/tool` renders the interactive graph builder with the canvas, 10 Code Blocks, 12 connections, inspector, explicit wiring controls, and export tabs. The packaged HTML contains a single deferred local script reference and no required external tag.

The local bundle interaction smoke test also passed: opening `Inspect & export` showed 10 nodes, 12 connections, 11 inputs, 10 outputs, one write action, schema checks, safety warning, and download controls. The JSON preview toggle is present and the graph is clearly marked for review because the sample contains a Revit write-back node.

Final bundle test after the universal router change succeeded. Opening `index.html#/tool` from disk renders the inspect-and-export state, with the expected controls and `.dyn` download button. A generated `renumber_rooms_level_03.dyn` was downloaded to `/home/ubuntu/Downloads` and validated by `validate-dyn.mjs`: required fields `Uuid`, `ElementResolver`, `Nodes`, `Connectors`, and `Views` were present; 10 Code Blocks, 11 connector records, and 10 view records passed the shape check. The final ZIP was rebuilt at approximately 206 KB with a deferred local IIFE script and README.
